#pragma once

/*
 * Copy this file to secrets.h (same folder) and fill in real values.
 * secrets.h is gitignored — never commit real WiFi passwords or API
 * hosts. config.h includes secrets.h automatically if it exists.
 */

#define WIFI_SSID       "your-wifi-name"
#define WIFI_PASSWORD   "your-wifi-password"
#define API_ENDPOINT    "http://your-backend-host/api/readings"
